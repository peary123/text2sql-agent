# Model client. The client also speaks Anthropic; install `anthropic` and set
# LLM_MODEL to a claude-* model to use it instead.
openai>=1.59.0

# Few-shot example retrieval.
scikit-learn>=1.4

# Serving.
fastapi>=0.115
uvicorn[standard]>=0.34

# HTTP client for the load test and the API tests.
httpx>=0.27
